<?php
    // página principal de la aplicación
    // validaremos que el usuario está autenticado ya que si no lo está debemos largarlo al login
    //desde esta página podremos acceder a las distitnas partes de nuetra aplicación


    // INCLUDES:
    //incluiremos en esta parte los includes necesarios para gestionar las sesiones.
    //si tuvieramos que usar acceso a los datos (BD) incluiriamos el modelo que necesitemos, llamando a la función correspondiente

    // CONTENIDO
    //validamos datos que nos lleguen por POST o GET si es que tenemos que validar algo
    //llamamos a una o varias funciones del modelo si es necesario

    //VISTA
    //incluimos la vista
?>